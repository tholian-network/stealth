
const RULE = {

	'font-face': (body, result) => {
	},

	'import': (body, result) => {
	},

	'keyframes': (body, result) => {
	},

	'media': (body, result) => {
	},

	'page': (body, result) => {
	},

	'supports': (body, result) => {
	},

	'viewport': (body, result) => {
	}

};


export default { RULE };

